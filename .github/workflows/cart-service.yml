name: cart-service

on:
  push:
    tags:
      - cart-service-*

jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - uses: ./.github/actions